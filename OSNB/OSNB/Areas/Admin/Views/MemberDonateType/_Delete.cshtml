@model OSNB.ViewModels.MemberDonateTypeViewModel
@using (Ajax.BeginForm("Delete", "MemberDonateType", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberDonateTypeSuccess",
            }, new { @id = "deleteMemberDonateTypeForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberDonateTypeDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member donate type ?
        </div>
    </div>
}
