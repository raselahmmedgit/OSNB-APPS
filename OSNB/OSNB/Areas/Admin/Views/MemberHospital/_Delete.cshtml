@model OSNB.ViewModels.MemberHospitalViewModel
@using (Ajax.BeginForm("Delete", "MemberHospital", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberHospitalSuccess",
            }, new { @id = "deleteMemberHospitalForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberHospitalDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member hospital ?
        </div>
    </div>
}
