@model OSNB.ViewModels.MemberDistrictViewModel
@using (Ajax.BeginForm("Delete", "MemberDistrict", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberDistrictSuccess",
            }, new { @id = "deleteMemberDistrictForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberDistrictDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member district ?
        </div>
    </div>
}
