@model OSNB.ViewModels.MemberBloodGroupViewModel
@using (Ajax.BeginForm("Delete", "MemberBloodGroup", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberBloodGroupSuccess",
            }, new { @id = "deleteMemberBloodGroupForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberBloodGroupDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member blood group ?
        </div>
    </div>
}
