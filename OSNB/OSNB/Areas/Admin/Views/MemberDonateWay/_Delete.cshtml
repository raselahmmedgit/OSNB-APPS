@model OSNB.ViewModels.MemberDonateWayViewModel
@using (Ajax.BeginForm("Delete", "MemberDonateWay", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberDonateWaySuccess",
            }, new { @id = "deleteMemberDonateWayForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberDonateWayDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member blood group ?
        </div>
    </div>
}
