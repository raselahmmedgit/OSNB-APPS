@model OSNB.ViewModels.MemberStatusViewModel
@using (Ajax.BeginForm("Delete", "MemberStatus", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteMemberStatusSuccess",
            }, new { @id = "deleteMemberStatusForm" }))
{  
    @Html.ValidationSummary(true)
    
    <div id="memberStatusDeletePage">
        <!-- Error Container -->
        <div class="row-fluid">
            <div class="span12">
                <div id="updateTargetId" class="error">
                </div>
            </div>
        </div>
        <div class="row-fluid">
            @Html.HiddenFor(model => model.Id)
            Do you want to delete this member status ?
        </div>
    </div>
}
